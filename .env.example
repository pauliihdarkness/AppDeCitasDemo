# Ejemplo de variables de entorno para Vite (archivo .env.example)
# Copia este archivo a `.env.local` o `.env` y reemplaza los valores por tus credenciales.

# Firebase configuration (utilizado en src/config/firebaseConfig.js)
VITE_FIREBASE_APIKEY=your-firebase-api-key
VITE_FIREBASE_AUTHDOMAIN=your-firebase-auth-domain
VITE_FIREBASE_DATABASEURL=your-firebase-database-url
VITE_FIREBASE_PROJECTID=your-firebase-project-id
VITE_FIREBASE_STORAGEBUCKET=your-firebase-storage-bucket
VITE_FIREBASE_MESSAGINGSENDERID=your-firebase-messaging-sender-id
VITE_FIREBASE_APPID=your-firebase-app-id

# Opcional: IDs de OAuth si usas proveedores que los requieren
# VITE_GOOGLE_CLIENT_ID=your-google-client-id
# VITE_FACEBOOK_APP_ID=your-facebook-app-id

# Notas:
# - Los nombres deben empezar con VITE_ para que Vite los exponga a import.meta.env
# - Nunca subas tus credenciales reales a un repositorio público. Usa .env.local o secrets en tu CI.
